Algoritmo "Área do triângulo"
Var
   base, altura, area: real

Inicio
escreval("Tamanho da base: ")
leia(base)
escreval("Tamanho da Altura: ")
leia(altura)

limpatela

escreval("Base = ", base)
escreval("Altura = ", altura)
area <- (base * altura) / 2
escreval("")
escreva("Área = ", area)

fimalgoritmo
